name: Build Android APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet pypdf

      - name: Flet Build APK
        run: |
          # Wir nutzen 'python -m flet', das ist auf GitHub sicherer
          python -m flet build apk --assets assets --verbose
          
      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: Bilacon-App-Release
          path: build/apk/*.apk
